# KNOWN_ISSUES — مشاكل مؤكدة (مع الأدلة)

> تُسجَّل هنا المشاكل المؤكدة بفحص مباشر أو مخرجات أمر فقط. لا شكوك نظرية.
> لا يُحذف بند إلا بعد إثبات الإصلاح في شجرة GitHub.

| # | المشكلة | الدليل | الحالة |
|---|---|---|---|
| 38 | **واجهة النقل كانت تتجمد بسبب Timer Gradio عالمي يرسل refresh كل ثانية أثناء الخمول بالتوازي مع heartbeat React** | إعادة إنتاج متصفح حي؛ فحص `ui.py` أظهر wiring لـ`gr.Timer(1.0)` على `queue.refresh` و`dashboard.refresh` | ✅ **مُغلقة في M31** — أزيل Timer العالمي، وبقي heartbeat React المشروط بالنقل وأزرار التحديث اليدوية؛ عقد UI وCI وفحص المتصفح الحي ناجحة |
| 1 | تضارب أرقام الإصدار داخل الحزمة (`config.py` = 3.1.0، `__init__.py` = 1.0.0/2.0) | فحص 2026-08-06 | ✅ مُصلَحة في الحزمة والمولد والنوت‌بوك واللوك |
| 2 | الريبو لا يحوي دستورًا محدَّثًا | مقارنة 2026-08-06 | ✅ مُصلَحة — `docs/CONSTITUTION.md` = v5.0.0 |
| 3 | لا بيت توثيق جذري | `ls` الجذر | ✅ مُصلَحة — ADR-001 |
| 4 | "177 passed" غير مُعاد إثباته بمثبتات `requirements.lock` الحالية | `docs/PHASE_REPORTS/PHASE_9.md` | **مفتوحة** — 299 passed محليًا وفي CI، لكن بيئة Colab الحية لم تُختبر بعد (M15-T01) |
| 5 | Gradio 6.20.0 و`prevent_thread_lock` لم يُبنيا في بيئة حقيقية | PHASE_9 "Not verified" | **مفتوحة** — M15-T01 |
| 6 | README الجذر بهوية قديمة "Drive Buddy" ورابط لريبو آخر | README.md | ✅ مُصلَحة |
| 7 | تحقق حقيقي (Telegram/Drive/نقل) غائب تمامًا | لا دليل Colab في الريبو | **مفتوحة** — M15-T01، بيد المالك |
| 8 | **CI كان يبني `teledrive_v3.1.zip`** بينما `AI_HANDOFF` و`TODO #2` ادّعيا تحديثه إلى v4.5 | شجرة `.github` عند HEAD 4cacc58 = `86abde40`، مطابقة لما قبل PR#3؛ blob `ci.yml` = `5dec51e` | ✅ **مُصلَحة** — commit `ff6a484` حدّث `ci.yml` إلى `teledrive_v4.5.zip`؛ تحققت في run `31243523514` ورُفعت الحزمة كـartifact |
| 9 | **الأرشيف الموزَّع يشحن مؤشرات بدل توثيق**: `INCLUDE_FILES` و`INCLUDE_DIRS` في `package_service.py` تشير إلى ملفات `python-package/` التي صارت مؤشرات سطر واحد بعد ADR-001 | أحجام blobs: `python-package/README.md` وCHANGELOG وHANDOFF وdocs/* كلها 205–454 بايت | **مفتوحة** — M14-T01 |
| 10 | **`launcher --check` كان يحل 22 إجراءً من 41 فقط** — الـ19 الباقية كانت بلا تصنيف موثق | baseline `61df83e`: `22/41`. التدقيق الكامل في `PHASE_15.md`، وفي **M13-T03 (PHASE_16)** أُضيفت اختبارات إثبات لإجرائي التحديد ورُقِّيا إلى READY فصارت النتيجة: `24/41 ready actions resolve` (6 `BLOCKED` و11 `NOT_TESTED`) | ✅ **مُغلقة في M13-T02 ومُحدَّثة في M13-T03** — 24 READY، 6 BLOCKED بسبب native Colab/Drive live gate، و11 NOT_TESTED، ولا dead/missing/unwired |
| 11 | **pytest لم يُشغَّل في جلسة PR#3** — البوابات المسجَّلة كانت compileall + launcher + notebook + cmp فقط | `docs/AI_HANDOFF.md` قائمة البوابات | ✅ أُغلقت في M12-T01 — 299 passed محليًا وفي CI (run 31243523514) |
| 12 | مخاطرة بنية تحتية: checkpoint تالف في بيئة تشغيل سابقة | رسالة المنصة | خارج الريبو؛ البيئة الحالية نظيفة |
| 13 | **CI لا يبدأ أصلًا (Invalid workflow file) على كل الفروع بما فيها main**: التعليق التوضيحي على run 31241947281 = `Invalid workflow file: .github/workflows/ci.yml#L1 (Line: 16, Col: 23): Unrecognized named-value: 'runner'. Located at position 1 within expression: runner.temp` — سياق `runner` غير متاح في `jobs.<id>.env` | runs 31129230384، 31237246480، 31239298725، 31241771183/31240323871، 31243142711 كلها 0s | ✅ **مُصلَحة** — commit المالك `ff6a484` استبدل `runner.temp` بـ `github.workspace`. أول run أخضر حقيقي `31243523514` نجح في 1m21s (Python 1m17s, Frontend 16s) |
| 14 | **تلوث بيت الذاكرة:** commit المالك `afde5fe` أنشأ `docs/pic for frontend` (بايت واحد، بلا محتوى أو مرجع) | `ls -la "docs/pic for frontend"` = 1 بايت؛ لا مرجع في أي ملف | ✅ **مُصلَحة** — حُذف في M12-T02 (PR #6) |
| 15 | **صلاحية المنصة:** تطبيق GitHub الخاص بـArena لا يملك `workflows`، فلا يستطيع تسليم أي تعديل على `.github/workflows/*` | `git push` → remote rejected refusing to allow a GitHub App to create or update workflow; REST API → HTTP 403 Resource not accessible by integration | ✅ **مُصلَحة بتطبيق المالك** — طبّق المالك commit `ff6a484` مباشرة على GitHub لفك العائق وتفعيل CI |
| 16 | **فخ غلاف GitHub Artifact:** تنزيل Actions يعطي `teledrive-package.zip` (غلاف يحوي `teledrive_v4.5.zip`) بينما Cell 1 كانت تقبل الملف الحقيقي فقط → `AssertionError` بعد mount، وإعادة التسمية تسبب `EOFError` (قراءة/كتابة على الملف نفسه) | محاكاة M15-T01 للسيناريوهات A–F من main النظيفة `1f60a37` على منطق Cell 1 حرفيًا | ✅ **مُصلَحة في M15-T02** — Cell 1 تكتشف الغلاف بالمحتوى وتستخرجه عبر temp + `os.replace` مع رفض traversal؛ 16 اختبارًا في `tests/test_restore_package.py` |
| 17 | **ظهور غير مشروط لحقلي OTP و2FA في واجهة Colab:** كان حقل الكود يظهر قبل `CODE_REQUESTED` وحقل 2FA يظهر دائمًا لجميع الحسابات حتى دون طلب كلمة مرور | فحص `ui.py` و `handlers.py` عند baseline 8fbd185 | ✅ **مُصلَحة في M15-T03** — تغليف اللوحتين في `gr.Column(visible=False)` واشتقاق الظهور ديناميكيًا من آلة الحالة؛ 15 اختبار contract في `tests/test_telegram_flow_contract.py` |
| 18 | **`ProgressTracker.snapshot()` يعلِّق نفسه (self-deadlock):** يمسك `threading.Lock` ثم يستدعي داخله `instant_speed()/average_speed()/eta_seconds()` التي تطلب القفل نفسه → تعليق دائم عند أول نداء؛ بقي كامنًا لأن `dashboard.refresh` غير جاهز ولا اختبار ينفّذ `stats.dashboard()` | إعادة إنتاج مباشرة في M15-T04: استدعاء `ProgressTracker().snapshot()` علّق العملية حتى timeout | ✅ **مُصلَحة في M15-T04** — `RLock` بدل `Lock` (سطر واحد)؛ تغطية تنفيذية عبر `tests/test_ui_shell_contract.py` الذي يستدعي `stats.dashboard()` كل render pass |
| 19 | **واجهة Gradio خام بلا RTL ولا ثيم مضمون ولا إدارة حالة إعادة رسم:** تبديل اللغة كان يحدّث صندوقًا فقط؛ Gradio 6 نقل theme/css إلى `launch()` (مسار deprecated عبر مُنشئ Blocks هو الوحيد المتاح من `ui.py` دون لمس `app.py`) | بناء حقيقي على Gradio 6.20.0 في M15-T04 (تحذير deprecation + غياب direction rtl) | ✅ **مُصلَحة في M15-T04** — قشرة غرافيت RTL/LTR عبر `gr.State` + `gr.render` مع بذر من الحالة الحية (`handlers.shell_seed`)؛ الثيم/CSS مثبتان عبر المسار deprecated ومحروسان باختبار |
| 20 | **اختبار contract قِلِق إحصائيًا أسقط بناء حزمة main بعد دمج PR #14:** `assert "abc" not in dumped` في `test_phone_code_hash_stays_in_memory_and_out_of_the_event_log` يصطدم عشوائيًا بمُعرِّفات UUID4 للأحداث التي تحوي السلسلة `abc` (مثل `abc91a3a-...`)؛ نفس الـcommit مرّر خطوة الاختبار وأخفق خطوة البناء (`1 failed, 379 passed`) فلم يُرفع أي artifact | سجل run `31326929948` / job `93278678720` (استُرجع عبر رابط موقَّت لمسار `GET /actions/jobs/{id}/logs`) + إعادة إنتاج محلي قبل الإصلاح (فشل عند التكرار 40 على uuid `fcaabbe1-abc8-...`) | ✅ **مُصلَحة في M15-T07** — sentinel بطول 32 خانة hex (التصادم يتطلب تطابق UUID4 تامًا ≈ صفر) + اختبار regression بحلقة 48 دورة تسجيل/خروج؛ 25 تشغيلة متتالية للملف خضراء؛ `402 passed` |
| 21 | **دورة تحديث حزمة Colab اليدوية:** تنزيل ZIP بدائيًا ورفعه إلى Drive وفتح نوت‌بوك جديد في كل تسليم، ولا نقطة تحديث مستقرة موثَّقة بـcommit/digest | DOC مهمة M15-T07 (قسم «Why this task exists») | ✅ **مُعالَجة برمجيًا في M15-T07 ومُفعَّلة نقطة النشر في M15-T08** — بوابة تحديث ما-قبل-الإقلاع في Cell 1: manifest موقَّت الإصدار من GitHub release مثبَّت (`pkg-2026.08.09-m15t07`) + تنزيل `.part` + تحقق sha256/الحجم + استبدال ذري + رفض أثناء تشغيل الـruntime؛ الإصدار المثبَّت **منشور فعليًا** (run `31343436790` success، نقطتا التنزيل تجيبان بلا مصادقة)؛ أرشيف CI والـDrive ZIP احتياطيان؛ إثبات Colab الحقيقي متبقٍ بيد المالك (M15-T01) |
| 22 | **تحليل Telegram دون نطاق محدد وتصفية وسائط:** الواجهة السابقة `scope=auto/message/chat` فقط، و`scan_link()` يفحص رسالة واحدة أو القناة كاملة بلا `range`/`latest` ولا `media_types` | فحص `ui.py` و`media_scanner.py` و`services.py` عند baseline `e4ba2ae` (M15-T11 §2) | ✅ **مُصلَحة في M15-T11** — `ScanRequest` مجمّد مع `validate()` المحدود (1000 رسالة، 1000 للمدى)، `scan_link(request)` مع `_iter_requested_messages` و`_matches_media_type`، `ScannerService.analyze(mode/start/end/limit/media_types)` المحدود، `Handlers.h_analyze_run` بـ7 مدخلات، واجهة جديدة `mode`/`media_types`/`message_id`/`start_id`/`end_id`/`limit` + `filter_media_types` المنفصل، و18 مفتاح locale؛ 16 اختبار contract؛ `25/41 ready` |
| 23 | **`launcher --check` يحل 24→25 إجراءً بعد M15-T11** | `python teledrive_launcher.py --check` قبل/بعد: `24/41` → `25/41 ready actions resolve` | ✅ **مُحدَّث في M15-T11** — `analyze.run` صار `implemented+tested` بدليل `tests/test_scoped_scan.py::test_handler_passes_bounded_scan_request` |
| 24 | **نقطة الإصدار المثبَّتة كانت تُقدِّم أصولاً من شجرة قديمة (M15-T07، بلا M15-T11)**: التاج `pkg-2026.08.09-m15t07` كان يستهدف `10b5d3b`، فلو شغّل المالك Colab قبل الإصلاح لاستقبل حزمةً بلا الفلاتر والمسح المُقيَّد | فحص `gh release view` قبل M15-T12: `target_commitish=10b5d3b` · `teledrive_v4.5.zip` `188695` بايت | ✅ **مُعالَجة في M15-T12** — أُعيد نشر التاج نفسه بأصول من الـmain الحالي `f8c0ec2` (يضم M15-T11): run `31345898521` success · `teledrive_v4.5.zip` `212474` بايت / sha256 `167d25d4…cce3` · manifest 378 بايت · `PUBLIC VERIFICATION OK` داخل الـrun · التفعيل الحقيقي على Colab متبقٍ بيد المالك (M15-T01) |
| 25 | **تبويب Analyze غير قابل للتشغيل من الواجهة الحية**: حقول `gr.Number` اختيارية بلا `value` وبـ`minimum=1` فيرسل المتصفح `0` فيُرفض الحدث قبل الـhandler (`Value 0 is less than minimum value 1.`)؛ الاختيارات إنجليزية خام داخل واجهة عربية؛ `InvalidLink` و`ValueError` يصلان للمستخدم كـ`err.unknown`؛ روابط الدعوة تنهار صامتة؛ تسمية مكررة (`btn.analyze` على الزر ومربع النتيجة)؛ كل الحقول ظاهرة بكل الأنماط | تشغيل Colab حي للمالك 2026-08-10 على `https://7860-…prod.colab.dev` (الأدلة A–F في ملفات M16) | ✅ **مُصلَحة في M16-T01** — `DEFAULT_SCAN_MODE="message"`، `analyze.set_mode`/`fields_for_mode()` تحكم رؤية الحقول، اختيارات مترجمة بقيم canonical، لا `minimum=`/`maximum=` أمامية (سلطة التحقق الوحيدة `ScanRequest.validate()`)، أخطاء مترجمة (`err.bad_link`/`err.link_invite_unsupported`/`err.scan_*`)، label `analyze.result`؛ بوابة T01 `97 passed` وكامل `443 passed` · launcher `26/42` · إثبات Colab الحي متبقٍ بيد المالك |
| 26 | **روابط الدعوة (`t.me/+hash` و`t.me/joinchat/…`) كانت مرفوضة حتى للحساب العضو** | اختبار حي معزول M27-T04: القناة الخاصة قابلة للحل والنقل، لكن `ScannerService` كان يرفض الرابط مبكرًا | ✅ **مُغلقة في M27-T04** — `CheckChatInviteRequest` يحل دعوة الحساب العضو فقط إلى InputPeer؛ لا Join تلقائي، وغير القابل للحل يرجع `err.private_channel_unresolved`. يلزم Colab حي لاحقًا فقط. |
| 27 | **توكن الوكيل كان بلا صلاحية `actions:write`**، فلم يكن يستطيع تشغيل `workflow_dispatch` تاريخيًا | محاولة 2026-08-10 بعد دمج PR #23 أعادت 403؛ لكن تحقق 2026-08-19 شغّل workflow النشر بنجاح (run `32296915067`) | ✅ **لم تعد قيدًا مثبتًا للجلسة الحالية** — نجح dispatch من CLI للحساب الحالي. لا يُفترض دوام الصلاحية دون تحقق مباشر في أي جلسة لاحقة. |
| 28 | **إجراءات غير الجاهز تختفي صامتًا من الواجهة**: `binder.button` كان يرسمها `visible=False` بلا سبب مترجم | جرد M17-T01 + launcher `32/42` بعد M17-T02 | ✅ **مُغلَقة في M17-T02-REST+M17-T03** — أضيف حقل `blocked_reason_key` على ActionSpec + `assert_complete()` صارمة + `binder.button` للأفعال المحظورة render visible-disabled بلوكيل مترجم؛ كل الأفعال الـ42 صارت ready (42/42 في launcher) فلا يوجد أي unready action حاليًا |
| 29 | **`settings.set_theme` تفضيل مخزَّن بلا أثر بصري**: لا light theme ولا إعادة رسم | فحص `services.py`/`ui.py` في M17-T01 | ✅ **مُغلَقة في M17-T03** — `ui_theme.py` يُقدّم PALETTES dark/light + BASE_CSS يعتمد على `var(--td-*)` + `theme_style_block(theme)` يُعيد `<style id="td-theme-vars">…</style>` يُحقَن في `gr.HTML` host؛ `h_settings_set_theme` يُبدّل الكتلة فعليًا ويثبت في SQLite |
| 31 | **تبويب Export يعرض مخرجات بلا زر ظاهر**: زر `export.build_zip` كان في الشريط العلوي ومخفيًا | فحص `ui.py` في M17-T01 | ✅ **مُغلَقة في M17-T03** — زر primary بارز في قسم التصدير + زر ثانٍ في الشريط العلوي؛ كلا الزرين موصولان لـ`export.build_zip` |
| 32 | **شريط تمرير التزامن كان يعرض قيمًا غير عقدية (حتى 19/50)** مخالفة للدستور (1–4 افتراضي 2) | الصور المرجعية + الدستور §5.1 | ✅ **مُغلَقة في M17-T03** — `gr.Slider(minimum=1, maximum=4, step=1, value=2)` مع تحقق server-side في `SettingsService` (1..4) ورفض القيم الخارجة برسالة مترجمة |
| 33 | **ألوان hardcoded في `ui.py`** بعد M15-T04 graphite shell | فحص `ui.py` قبل M17-T03 | ✅ **مُغلَقة في M17-T03** — zero hardcoded colors في `ui.py`؛ كل الألوان عبر CSS variables من `ui_theme.py` |
| 34 | **chips الحالة العليا كانت تعرض نصوصًا ثابتة** بدل حالة الاتصال الفعلية | فحص `ui.py` قبل M17-T03 | ✅ **مُغلَقة في M17-T03** — 4 chips حقيقية من `ctx`: telegram_label/drive_label/current_folder_name/dash.engine_colab؛ عند الانفصال تعرض «غير متصل» بلون warn |
| 35 | **checkpoint فاسد يمكن أن يُلحق ضررًا** عند الاستعادة (كانت restore_from_drive/disk تمرر JSON غير متحقَّق منه) | فحص `checkpoint_manager.py` قبل M17-T03 | ✅ **مُغلَقة في M17-T03** — `validate_snapshot()` + `InvalidCheckpointError` + رفض برسالة مترجمة `msg.recovery_corrupt`؛ لا `rmtree`، لا auto-resume |
| 36 | **`logs.*` كانت تُصدِّر/تُنسَخ نصوصًا خامًا** قد تحتوي api_hash/phone/token/email/path حساس | redaction.py قبل M17-T03 كان regex بسيطًا ويطابق `code: str` كاذبًا | ✅ **مُغلَقة في M17-T03** — redaction مُحكَّم (emails، phone، KV للأسرار مع تجنّب Python kwargs/annotations، مسارات حساسة) + filter per-level على `[LEVEL]` token |
| 37 | **`bun run lint` / `bun run build` لم تكونا قابلتين للتشغيل في بعض sandboxes القديمة** | جلسات DOC-37 السابقة بلا Bun | ✅ **مغلقة كحاجز تسليم** — M24 ثبّت الواجهة فعليًا عبر GitHub Bun في push run `31640781460` وpull_request run `31640785475`؛ كلا Frontend build ناجح على SHA `56a285b` |
| 38 | **Live Colab proof غائب** لكل المراحل حتى الآن | لا وصول لـColab حي من الساندبوكس | **مفتوحة (بيئة)** — بيد المالك؛ M15-T01 |
| 39 | **Drive upload retry/timeout وطلب إغلاق Cell 7 أثناء النقل يحتاجان تعديل سطح نقل محمي أو عقد Cell 7 المحمي** | DOC-38 §5.2 يمنع صراحة لمس `transfer_manager.py` عند الحاجة؛ وCell 7 يستدعي `ctx.shutdown()` بلا `force=True` أو تأكيد. لم تُلمس الملفات المحمية. | **مفتوحة — تحتاج تفويضًا صريحًا للملفات المحمية** |
| 40 | **«خطأ غير معروف. جرّب مرة أخرى. [cid]» عند ربط Telegram ثم عند «إرسال الكود» (بلاغا المالك `d75de588` ثم `fd41da8b`)** | M18-T02: إعادة إنتاج محلية → `IncompleteReadError` يفلت من `set_credentials` غير المحمي. M18-T03: `_handle_send_error` لم يكن يصنّف سوى `FloodWaitError` — `auth.sendCode` أول استدعاء يحمل زوج api_id/api_hash فكل رفض (`ApiIdInvalidError`/`PhoneNumberInvalidError`/`PhoneNumberFloodError`/نقل) كان ينتهي `err.unknown` | ✅ **مُغلقة بالكامل** — M18-T02 صنّف connect على مستوى الواجهة؛ **M18-T03 (بتفويض المالك للمس `telegram_auth.py`)** أتمّ التصنيف العميق: `_handle_send_error` يسمّي `err.bad_api_pair`/`err.tg_phone_invalid`/`err.tg_phone_flood`/نقل→`err.tg_connect_failed`، `_handle_code_error` يبقي الهاش عند فشل النقل، `verify_password` لا يسمّي النقل/flood «كلمة مرور خاطئة» بعد الآن؛ `589 passed` · launcher 45/45 · PHASE_M18_T03. تبقى أصناف RPC نادرة في fallback `err.unknown` عمدًا حتى تظهر في سجل حي؛ إعادة نشر التاج بيد المالك (#27) |
| 41 | **الإثبات الحي في Colab لإصلاحي M18-T02/T03 غير مكتمل** — يتطلب: دمج PR M18-T03، إعادة نشر التاج من main الجديد، Restart runtime ← Cell 1 ← 2–4، ثم محاولة «إرسال كود» حقيقية (مطلوب منها الآن رسالة مسمّاة بدل `err.unknown`) | بلاغ المالك `fd41da8b` وقع على حزمة M18-T02 المؤكَّدة النشر — أي أن التسليم حتى M18-T02 مثبت، وM18-T03 ينتظر الدورة نفسها | **مفتوحة — بيد المالك** (امتداد #38/#7: M15-T01) |
| 42 | **الثيم النهاري ليس الافتراضي** رغم أن M19-T01 §5.2 يطلبه — الافتراضي `"dark"` مُصلَّب في `PreferencesService` (`services.py`) وفي `shell_seed` (`handlers.py`) | فحص المصدر 2026-08-12 | ✅ **مُصلَحة في M20-T02** — بتفويض المالك: `services.DEFAULT_THEME = "light"` مصدرًا واحدًا يقرأه `PreferencesService` و`shell_seed`، و`teledrive/theme.py` يعيد تعريف كل متغيرات Gradio تحت `:root` و`.dark`/`body.dark`/`.gradio-container.dark` بـ`!important` + `color-scheme: light`، مع `MutationObserver` يشطب صنف `dark` باستمرار عبر `head=`/`js=` في `launch()`. لا مسار (متصفح داكن، `?__theme=dark`، إطار Colab داكن) يستطيع تسويد الواجهة. الاختبار: `test_ui_colab_render_contract::test_light_theme_is_the_default_and_dark_can_no_longer_win` |
| 43 | **الوضع النهاري والتتابع 1→5 لم يُثبتا في Colab حي** — M20-T02/T03 مثبتان بالبناء والاختبارات وخادم Gradio حقيقي داخل الساندبوكس، لكن لا متصفح Colab ولا لقطة من المالك | جلسة M20: `629 passed` + خادم حي على `0.0.0.0:7860` + فحص `/config` و`HTML` المُقدَّم | **مفتوحة — بيد المالك**؛ الحالة الصادقة: Code-complete candidate، لا `Colab-ready` ولا `Complete` |
| 44 | **التزامن فوق 8 غير مختبر مقابل ذاكرة Colab الحقيقية وحدود تيليجرام** — ADR-0001 رفع السقف إلى 100 بقرار المالك؛ المخاطر (RAM عند 8 MiB/عنصر، ضغط القرص، FloodWait، Drive 403) مقبولة صراحةً وغير مُخفَّفة | ADR-0001 §Consequences · `tests/test_concurrency.py` يثبت الحد الرياضي فقط (سيمافور 100/250 مهمة) | **مفتوحة بقبول المالك** — الواجهة تعرض تحذيرًا إجباريًا فوق 8 (`warn.concurrency_high`) |
| 45 | **براهين M20 على مستوى الربط (Fake-tested) لا التكامل الحي** — `tests/test_ui_contract_proofs.py` (18 اختبارًا) يثبت أن كل handler يصل إلى نفس `service_path` المعلن، ولا يثبت جولة Telegram/Drive حقيقية | الملف نفسه + Constitution §11 | **مفتوحة** — نفس بند M15-T01 (الإثبات الحي بيد المالك) |
| 46 | **React bridge مثبت داخل Gradio محليًا وCI فقط، لا داخل Colab حي** — `ReactPanel(gr.HTML)` + جولة submit حقيقية نجحت في sandbox، لكن لم تُشغّل الخلايا 1..7 ولم يحدث Telegram/Drive/transfer حي | M24 code SHA `56a285b` · push `31640781460` · PR `31640785475` · `PHASE_M24_T03_COLAB_SMOKE.md` | **مفتوحة — بيد المالك**؛ الحالة `Code-complete candidate` لا `Colab-ready` |
| 47 | **حد الأسرار يفرض بقاء مصادقة Telegram الحساسة في حقول Gradio الآمنة** — generic React bridge يرفض api_id/api_hash/phone/code/password/session/token، لذلك لوحة React لا تجمعها؛ الواجهة القديمة محفوظة داخل Accordion للمصادقة فقط | `react_bridge.py::_SECURE_GRADIO_ONLY/_SECRET_KEYS` + `test_react_bridge_rejects_blocked_action`/`does_not_log_credentials` | **مفتوحة كإثبات UX حي** — التصميم مقصود أمنيًا، ويحتاج فحص المالك أن الانتقال بين Accordion وReact واضح في Colab |
| 48 | **لقطات M24 الثلاث غير موجودة** (1280×768، 768×768، 390×844) | لا browser screenshot engine في جلسة Arena؛ CSS/SSR فقط | **مفتوحة — بيد المالك**؛ لا يُستعاض عنها بصورة static أو ادعاء نصي |
| 49 | **بعد Restart تبقى صفوف SQLite القديمة وStart كان يرفض البدء** لأن التحديد في الذاكرة فارغ و`start_selected` لا يعالج الجدول كله | بلاغ المالك (191 في الانتظار / 0 قيد التنفيذ / Start صامت) + فحص `queue_manager.selected_pending` عند baseline `0c394a8` | ✅ **مُصلَحة في M25-T01 (PR #44)** — Start بلا تحديد يلتقط كل Pending؛ Stop يعرض مسح غير المكتمل؛ التجميع بالقناة+التاريخ. الإثبات الحي بيد المالك بعد إعادة نشر التاج |
| 50 | **بلوب ADR-004 القديم `td_telegram.session.vault` كان خزينة منافسة**؛ من يملكه مع `api_hash` يستطيع انتحال الحساب | تشخيص M24-T05 أظهر وجود البلوب القديم مع ملفي الخزينة الحديثة في المجلد نفسه | **مُعالجة في الكود بـM24-T05** — مسار التفويض يكتب الخزينة الحديثة فقط وlogout/forget يحذفان البلوب القديم. إثبات التنظيف على Drive حي مؤجل إلى M24-T06 |
| 51 | **Keep-alive لا يخلّد Colab** — نبض + نقرة Connect تؤخّر الخمول فقط | حد Google: ~90 دقيقة خمول / ~12 ساعة فري؛ التاب المغلق يفصل | **مفتوحة بتصميم** — ليست عيبًا |
| 53 | **إذا أبطل Telegram مفتاح جلسة محفوظة مثل `AuthKeyDuplicatedError`، يجب أن يعود المستخدم إلى دخول جديد من دون فقد زوج الاسترداد القديم قبل نجاح البديل** | تشخيص M31 ثم M32-T01 | ✅ **مُغلقة في M32-T01 ومندمجة ومنشورة** — PR #71 عند `5255889`، CI `4/4`، ونشر #32327688915. زوج جديد بإصدار وmanifest لا ينشط إلا بعد اكتمال رفعه؛ الجلسة المرفوضة تنظف محليًا فقط وتعود إلى `READY_FOR_PHONE`، بينما يبقى Drive حتى يسجل دخول جديد مصرح به. اختبار Colab حي لهذه الدورة ما زال ضمن #56/M24-T06. |
| 52 | **الواجهة لا تتحدّث ذاتيًا أثناء النقل** — الشريط وكل الأرقام تتجمّد على آخر لقطة حتى ضغطة `تحديث` (بلاغ المالك 2026-08-13) | تصميم الجسر: لقطة تُطلب فقط عند إجراء صريح | ✅ **مُصلَحة ومثبتة حيًا في sandbox في M28-T01، ومحسّنة في M28-T02** — نبض هادئ كل ثانية ينادي `queue.refresh` ويستبدل اللقطة كاملة أثناء `running`/in-flight؛ صف فيديو حقيقي ظهر عند `Downloading 55%` ثم `Uploaded 100%` بلا زر تحديث، وظهر الملف في Drive بتحقق مستقل. يبقى إثبات Colab بعد إعادة نشر الحزمة (#27). |
| 54 | **`telegram_creds.json` كان يحفظ `api_hash` صريحًا بجوار نسخة خام من `telegram.session` داخل `TeleDrive_AppData`** | قرار M24-T03 السابق، وتشخيص M24-T05 الذي أظهر الخزينة الحديثة مع بلوب ADR-004 القديم في المجلد نفسه | **مُعالجة في الكود بـM24-T05** — صيغة 2 الافتراضية تغلّف الجلسة ولا تضع `api_hash` في JSON؛ صيغة 1 للقراءة فقط و`TELEDRIVE_VAULT_PLAINTEXT=1` اختيار مالك صريح. التحقق الحي من الترقية والحفظ بيد المالك في M24-T06 |
| 56 | **التحقق الحي لخزينة M24-T05 غير مكتمل** — يلزم إثبات الحفظ التلقائي بصيغة 2، الاستعادة بعد VM جديد، التنظيف عند logout، وعزل حساب Drive مختلف | STEP 0 من المالك أثبت Telegram وDrive متصلين مع وجود جلسة SQLite صحيحة وخزينتي ADR-004/الجديدة؛ لا يثبت وحده سلامة الحفظ أو الاستعادة بعد إعادة التشغيل | **مفتوحة — بيد المالك (M24-T06)**؛ الحالة تظل Implemented + fake-tested، لا Colab-ready أو Complete |
| 55 | **[OPEN · M24-T03] `binder.load` / `load_sync` قد لا يربطان أي page-load action لأن `_page_loads` تُملأ داخل دالة `@gr.render`** — قياس مباشر في جلسة M24-T03 أظهر `F1PROBE_FINAL_PAGE_LOADS 0` بعد `ui.build(ctx)` | اختبار مؤقت في جلسة M24-T03: `UIBinder.load_sync` يُستدعى بجسم `@gr.render` الذي يؤجله Gradio للمتصفح، فلا page-load actions مسجلة وقت النداء في العملية | **مفتوحة — تحتاج تحقيقًا لاحقًا** — الاستعادة لم تعد تعتمد عليهما (`autorestore_once` في `ui.build` قبل الرسم)، لكن الآلية نفسها قد تكون ميتة؛ المعالجة في مهمة لاحقة |
| 57 | **Pause/Stop/Resume أثناء النقل كان لا يوقف الملف الجاري فعليًا** — أعلام asyncio غير آمنة بين الثريدات ولم تكن callbacks تفحصها | تشخيص M26-T01، ثم M27-T04 أثبت Pause/Resume/Stop على ملف Telegram وDrive حقيقيين في sandbox معزول | ✅ **مُثبت حيًا خارج Colab في M27-T04** — Pause يحفظ `.part`، Resume يصل `Uploaded`، وStop يبقى `Stopped` بلا ملف Drive جديد. يبقى اختبار Colab الحقيقي فقط. |
| 58 | **Resume كان يعيد الملف الجاري من البداية ولا يستأنف من offset** رغم بقاء `.part` المحلي | قرار نطاق M26-T01، ثم M27-T04: Pause حقيقي أنشأ `.part` واستؤنف تنزيل Telethon | ✅ **مُثبت حيًا خارج Colab في M27-T04** — `download_partial()` استأنف من offset محاذٍ وانتهى إلى `Uploaded`، بلا حذف `.part` أو ملف Drive. يبقى اختبار Colab الحقيقي فقط. |
| 59 | **تأخر Pause/Stop مقيد بزمن chunk واحد** على شبكة بطيئة | تصميم M26-T01 تعاوني؛ لا إلغاء قسري للمهام وسط API call | **مفتوحة بتصميم** — يحفظ اتساق الصفوف ولا يترك worker بلا مالك. |
| 60 | **لا تقاطع في Verifying أو UploadedPendingCheckpoint** | قرار M26-T01: الملف الموجود على Drive يجب أن يمر بالتحقق والـcheckpoint كي لا يصبح يتيمًا | **مفتوحة بتصميم** — Pause/Stop تسريان في التحميل والرفع فقط. |
| 61 | **التحديث التلقائي للنسبة والقنوات الخاصة كانا خارج نطاق M26-T01** | فصل DOC: M26-T02 للـTimer وM26-T03 لحل كيان Telethon | **مُعالَج ومثبت جزئيًا حيًا خارج Colab** — M28 يثبت refresh، وM27-T01 يضيف حل entity وهوية `-100<id>` ورسالة عدم الوصول. تحقق M29-T01 استخدم رابط دعوة قناة خاصة للحساب العضو ونقل Telegram→Drive فعليًا؛ يبقى اختبار Colab فقط. |
| 62 | **كتابات SQLite للتقدم لكل chunk كانت قد تضخم حمل قاعدة البيانات** | فحص مسار callback في `transfer_manager.py` قبل M27-T01 | **مُعالجة في M27-T01 (PR #54 مدمج وCI أخضر)** — throttle `0.5s` لكل عنصر مع flush عند حدود المراحل/النهاية؛ مثبت بـfake tests، لا قياس أداء Colab حي بعد. |
| 63 | **انهيار عامل نقل غير متوقع كان يمكن أن يبدو كاكتمال صامت** | `asyncio.gather(..., return_exceptions=True)` قبل M27-T01 كان يحجب exception عن callback انتهاء المحرك | **مُعالجة في M27-T01 (PR #54 مدمج وCI أخضر)** — تُعاد الأخطاء غير الملغاة إلى `QueueManager._on_run_done` الذي يسجل `transfer run crashed` ويعيد `idle`؛ مثبت باختبارات محلية فقط. |
| 64 | **لوحة React لم تكن تحدّث تقدم النقل تلقائيًا رغم وجود النبض في المصدر** | M27-T02 أصلح heartbeat؛ M27-T04 كشف أيضًا أن البندل يرمي `process is not defined` ويمنع اللوحة من التحميل | ✅ **مُعالجة ومثبتة في متصفح محلي في M27-T04** — بندل Production جديد بلا `process.env.NODE_ENV` واللوحة تظهر كاملة بلا console error. يلزم دمج وإعادة نشر حزمة Colab وRestart لاختبار Colab. |
| 65 | **Pause/Resume للطابور الفارغ كانا يبلّغان حالة محرك مضللة:** Pause يكتب checkpoint ويعلن `paused` بلا drain، وResume يعلن `running` بلا صف مستأنف أو عامل | اختبار استخدام حي محلي: طابور 0 صفوف تحولت شارته سابقًا `paused` ثم `running` | ✅ **مُغلقة في M27-T05 (PR #61)** — Pause الخامل no-op بلا checkpoint، وResume يشتق الحالة من drain حقيقي؛ Python regression + UI حي أبقيا الشارة `idle` |
| 66 | **تحقق Analyze الأمامي كان يوقف الطلب بصمت:** `validateAnalyzeInput()` يعيد reason code، لكن `AnalyzeSection` كان يعود من دون إشعار؛ ومع بعض أخطاء الخدمة أبقى bridge `Action completed` عامًا | اختبار UI حي: إدخال ناقص يترك إشعار نجاح قديم ولا يفسر السبب | ✅ **مُغلقة في M27-T05 (PR #61)** — إشعار React مترجم للرابط/الرسالة/النطاق/الحد، ووسم أخطاء التطبيق يجعل bridge يعيد non-success؛ عقود Python/React وUI حي |

| 68 | **عبر المنصات (Windows): (أ) `safe_disk_free()` كانت تعتمد `os.statvfs` غير الموجود على Windows فتعيد 0 داخل except عمياء ⇒ كل نقل يفشل `disk_full` والصفوف تبقى `Pending` (آلة الحالات ترفض `Pending→Failed` صامتة)؛ (ب) `is_mounted_drive()` كانت تستخدم `str(Path)` الذي يشوّه بادئات `/content/drive` إلى باكسلاش على Windows فيُلغى حارس `MountedRootError` لمنع SQLite على Drive/FUSE** | إعادة إنتاج مباشرة على Windows/Python 3.11: pytest `19 failed, 727 passed` قبل الإصلاح، `safe_disk_free('.')`=0، و`_process` صنّفت `disk_full` مع `try_transition→None` | **مُصلَحة في M34-T01** — fallback `shutil.disk_usage().free` + `Path.as_posix()`؛ بعد الإصلاح `746 passed` · launcher 51/51 · notebooks identical · commit `c85ee9f` على `fix/m34-t01-cross-platform-disk-and-mount-guards`؛ تأكيد CI بعد دمج PR |
| 67 | **README الجذر متناقض مع الدستور v5.0.0:** (1) يذكر "TeleDrive v4.5 / v4.5.0" كهوية المواصفة بينما الدستور v5.0.0؛ (2) يدّعي "مهمة M15-T01 متبقية" بينما مدموجة في `main`؛ (3) يقول "هذا المشروع متصل بـ Lovable" كمنفّذ بينما الدستور §3 يقضي بخروج Lovable نهائيًا | فحص README.md قبل PR #73: السطر 1/3 = v4.5، السطر 19 = M15-T01 متبقية، السطر 177 = Lovable كمنفّذ | **مُغلقة في M33-T01 (PR #73)** — حُدِّثت الهوية إلى v5.0.0 (مع الإشارة لموروث المنتج v4.5.0)، صوّبت وصف M15-T01 (مدموجة فعليًا؛ التحقق الحي بيد المالك)، واستُبدل قسم "المزامنة مع Lovable" بقسم "التطوير والحوكمة" يقرّ بأن Lovable خرج نهائيًا والمنفّذ الوحيد LM Arena Agent. لم يُمسَّا أسماء الـartifacts المنشورة (`teledrive_v4.5.zip`، `pkg-2026.08.09-m15t07`) لأنها أسماء الحزمة الفعلية لا إصدار الحوكمة |
